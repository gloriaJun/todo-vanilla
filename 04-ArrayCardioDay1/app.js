const appView = (function () {
  return {
  }
})();

const appController = (function () {
  return {
    init() {
    }
  }
})();

document.addEventListener('DOMContentLoaded', () => {
  appController.init();
});
